use types::error::Error;

pub async fn insert_quote() -> Result<(), Error> {
    // // Insert a quote
    // let author = "me";
    // let quote_text = "Hello, MSSQL!";
    // let new_id = Uuid::new_v4();
    //
    // client.execute(
    //     "INSERT INTO quote (id, author, quote) VALUES (@P1, @P2, @P3)",
    //     &[&new_id, &author, &quote_text],
    // ).await?;

    Ok(())
}
